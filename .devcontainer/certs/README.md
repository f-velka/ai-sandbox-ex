# 組織CA証明書の投入場所

組織のプロキシ(Zscaler等)が外向きTLSを再署名する環境では、そのCA証明書(`.pem`または
`.crt`)をこのディレクトリへ置いてからイメージをビルドする。
agentとdindのイメージがトラストストアへ取り込み、上流のTLS検証を通せるようにする。

再署名のない環境では何も置かなくてよい。
証明書はホスト固有ファイルとしてgit管理の対象外にしている。
