[project]
name = "ai-sandbox-ex"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=1.13",
    "pytest>=8.0",
    "ruff>=0.12",
]

[tool.uv]
package = false
